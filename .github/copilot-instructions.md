# Copilot Instructions for Api-Specs

## What This Repo Is

This repository is a curated catalog of API specifications in various formats. Specs are used for demos, testing, and development. They may be added manually or generated by Copilot.

## Repository Structure

```
rest/
  openapi/          # OpenAPI 3.0.x specs (.json or .yaml)
  wadl/             # WADL specs (.wadl, XML-based)
soap/
  *.wsdl            # WSDL specs (.wsdl, XML-based)
```

## Naming Conventions

All spec files follow the pattern:

```
<api-name>-<YYYY-MM-DD>.<ext>
```

- `<api-name>` — lowercase, hyphen-separated (e.g. `banking`, `supply-chain`, `hr`)
- `<YYYY-MM-DD>` — the date the spec was created
- `<ext>` — one of `json`, `yaml`, `wadl`, `wsdl`

Examples: `banking-2026-04-18.json`, `hr-2026-04-18.wsdl`

## Spec Authoring Guidelines

### OpenAPI (REST)

- Use **OpenAPI 3.0.0 or 3.0.1** only. Do **not** use 3.1.x.
- Include `info.title`, `info.version`, and `info.description`.
- Define `servers` with at least one URL.
- Use `tags` to group operations logically.
- Define reusable schemas under `components/schemas`.
- Include request/response examples where practical.
- Use `bearerAuth` security scheme unless the API is explicitly public.
- YAML is preferred but JSON is acceptable.
- **Azure API Management compatibility** — specs must be importable into Azure APIM:
  - All `$ref` pointers must be internal (no external file references).
  - Parameter names must be unique (case-insensitive) across path, query, and header.
  - No `requestBody` on `GET`, `HEAD`, or `OPTIONS` operations.
  - Do not rely on custom vendor extensions (`x-` properties); APIM ignores them.
  - Keep each API URL path under 128 characters.

### WADL (REST, XML)

- Use the `http://wadl.dev.java.net/2009/02` namespace.
- Include a `<doc>` element with a title and description.
- Define schemas inline under `<grammars>`.
- Use `<resource>` elements to define endpoints and nest sub-resources logically.
- Include `<method>` elements with `<request>` and `<response>` children.

### WSDL (SOAP)

- Use WSDL 1.1 with SOAP 1.1 binding.
- Define all types inline under `<wsdl:types>` using XSD.
- Use document/literal style.
- Name operations clearly (e.g. `GetEmployee`, `CreateOrder`).
- Include `<wsdl:service>` with a descriptive endpoint URL.

## General Rules

- Specs should represent **realistic, enterprise-grade APIs** — not trivial hello-world examples.
- Each spec should cover multiple resources/operations (aim for 5+ endpoints or operations).
- Use realistic field names, types, enumerations, and constraints.
- Do not include real credentials, secrets, or PII in specs.
